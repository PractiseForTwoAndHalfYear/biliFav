# AGENTS.md

本文件是 Codex 在 `C:\AI_Lab\vibe_coding\biliFav-main` 项目内工作的项目级规则。除非用户明确覆盖，所有自动化修改、排查、测试、提交说明都应遵守这里的约定。

## 项目概况

- 项目名称：`biliFav`，用于输入 B 站 UID 后整理收藏视频，辅助分类、标记、每日观看/收听计划和评级。
- 当前项目形态：单文件前端原型 + 项目搭建说明 + 全栈架构文档。
- 当前没有 `package.json`、真实 `src/` 目录、构建工具、测试框架或 Git 仓库元数据。不要假设存在 `npm test`、`npm run build`、Vite 工程、后端服务或远端仓库。
- 当前目录下的主要文件：
  - `README.md`：项目简介。
  - `bili-fav-html.html`：基础单文件 React 原型，依赖 CDN、Babel 和 Tailwind CDN。
  - `bili-fav-fast.html`：当前更完整的单文件版本，内置样式和图标，包含“评级”视图。优化当前可用页面时优先以此文件为主版本。
  - `bili-fav-vite.md`：迁移到 React + Vite + Tailwind 的搭建说明。
  - `bili-fav-skeleton.md`：最小可跑全栈骨架说明，包含 Express + TypeScript + Prisma + PostgreSQL + JWT。
  - `bili-fav-fullstack.md`：完整全栈架构设计说明。
- 本项目当前适合直接双击 HTML 预览；如果浏览器安全策略、CDN 加载或后续跨文件加载需要本地服务，再使用简单静态服务。
- 后续如果用户要求“落地真实工程”，优先从 `bili-fav-skeleton.md` 的最小骨架开始，不直接跳到复杂全栈版本。

## 沟通语言

- 日常沟通必须使用中文，直奔主题，优先说明结论、改动和验证结果。
- 技术报错、异常名、API 名称、命令输出、库名、配置键、HTTP 状态码、接口路径等保留英文原文。
- 如果出现错误，不要只翻译或概括；必须保留关键英文错误信息，方便用户复制到搜索引擎查询。
- 解释技术方案时可以中文为主，英文术语保留原名，例如 `React`、`Vite`、`localStorage`、`fetch`、`Prisma`、`PostgreSQL`、`JWT`。
- 用户英文基础较弱，避免要求用户用英文补充需求；需要确认时用中文问一个关键问题即可。
- 搜索资料、排查报错、查询官方文档时可以使用英文关键词，输出给用户时用中文解释。

## 工作方式与用户偏好

- 默认以效率和结果为导向，不频繁确认，不把简单事项拆成多轮询问。
- 需求足够明确时直接执行；只有在继续操作会带来明显风险、不可逆影响或与当前规则冲突时才先询问。
- 中间反馈要短，说明正在做什么、发现了什么、下一步是什么。
- 不要为了显得顺利而掩盖问题。无法完成、验证失败、环境缺失、网络失败都要真实说明。
- 不要编造不存在的测试、构建结果、Git 远端、依赖版本、浏览器验证或后端接口。
- 用户明确说“复盘”“日报”“不要修改任何文件”时，只输出分析和计划，不改文件。
- 只有在用户明确说“执行第 X 项”“开始修改”“帮我实现”“落地代码”等同等指令后，才开始修改项目功能代码。
- 如果用户要求更新 `AGENTS.md`、文档或规则文件，这属于明确文档修改指令，可以直接修改对应文档。

## 编码与文件规范

- 所有文本文件使用 `UTF-8` 保存，尤其是中文页面内容、标题、说明文案、Markdown 文档和 `AGENTS.md`。
- 修改中文乱码时，要先确认真实文件编码和浏览器显示结果，并保留 `<meta charset="UTF-8">` 或等价声明。
- 在 PowerShell 中读取中文文件时，必要时显式使用 `-Encoding UTF8`。
- 当前阶段优先保持“单文件原型可直接打开”的可用性。页面相关 CSS 和 JS 可以继续放在同一个 HTML 中，除非用户明确要求拆分或迁移工程。
- 不引入构建链路、框架、打包器、数据库或复杂依赖，除非用户明确要求。
- 保持代码简洁，避免为了小功能创建过多抽象。
- 变量和函数命名应表达业务含义，例如 `buildDays`、`autoBucket`、`toggleWatched`、`setRating`，避免 `a`、`data1`、`temp2` 这类含义不明的命名。
- JS 逻辑应按职责分组：存储适配、B 站请求、数据分类、渲染组件、事件处理、设置与状态持久化。
- CSS 改动优先复用已有样式体系；不要无节制堆叠重复样式。
- 避免无限制增加文件体积。单个 HTML 文件如果继续明显膨胀，应优先考虑整理结构、删除重复样式、合并重复函数，再新增功能。
- 不要删除现有视觉效果、交互、分类逻辑、评级逻辑或本地存储逻辑，除非需求明确要求替换。

## 前端与产品规则

- 这是 B 站收藏整理工具，不是营销落地页。页面优先服务“整理、筛选、标记、计划、回看”这些高频操作。
- 前台页面优先级高于后台和架构设计：先保证用户能打开、能拉取收藏、能分类、能标记、能恢复本地状态。
- 页面应在桌面浏览器上优先表现良好，同时不能在常见移动宽度下出现明显文字重叠、按钮溢出或布局崩坏。
- 使用 Grid、Flex、`minmax()`、合理换行、截断和固定交互区域，保证视频卡片、筛选栏、标签、按钮在窄屏下稳定。
- 不要让文本溢出按钮、卡片、标题栏或统计区域；必要时使用换行、截断或缩小容器内字号。
- `bili-fav-fast.html` 当前依赖浏览器本地 `localStorage` 保存 `config`、`videos`、`overrides`。涉及数据迁移、清空、重置时必须谨慎。
- B 站 API 请求当前通过第三方 CORS 代理访问，稳定性和隐私风险都要在复盘或说明中提醒。
- 不要把单文件原型强行改成复杂全栈工程，除非用户明确选择“落地全栈骨架”或类似任务。

## 安全与红线

- 禁止在代码中硬编码个人密码、账号凭据、API Key、Access Token、Refresh Token、Cookie、私钥或真实业务密钥。
- 禁止提交 `.env`、`.env.local`、`.env.*`、密钥文件、导出的浏览器 Cookie、真实用户数据或本地机器敏感路径。
- 如果需要示例配置，只能提交 `.env.example`，并使用占位符，例如 `YOUR_API_KEY_HERE`。
- 不要把用户个人信息、真实收藏隐私、真实账号 Cookie 或内部数据写入演示页面。需要数据时使用模拟数据。
- 不删除用户数据，不清空 `localStorage`，不重置数据库，不批量删除文件，除非用户明确要求并确认目标范围。
- 引入外部 CDN、代理或第三方脚本前，要考虑可用性和隐私影响；不要加入未知来源脚本。
- 不执行破坏性命令，例如 `git reset --hard`、批量删除、清空目录，除非用户明确要求并确认目标路径。

## 测试与验证

- 开发前优先思考可验证行为；能用最小页面验证时，不只做静态阅读。
- 当前项目没有测试框架，因此每次修改后至少做与改动匹配的真实验证：
  - HTML 结构检查：确认标签闭合、入口脚本存在、关键容器如 `root` 未丢失。
  - 编码检查：确认中文在文件读取和浏览器中显示正常。
  - 浏览器验证：打开被修改页面，检查控制台是否有 `ReferenceError`、`TypeError`、`SyntaxError` 或 CDN 加载失败。
  - 视觉验证：检查桌面和窄屏宽度下是否有重叠、溢出、空白区域、不可读文字。
  - 交互验证：涉及筛选、标记已看、加精、评级、设置保存、每日清单时，至少验证对应核心路径。
  - 数据验证：涉及 `localStorage` 时，确认不会误删已有 `biliFav:*` 数据。
- 涉及 B 站 API 拉取时，不要使用用户隐私 Cookie；如果只需要验证页面逻辑，优先使用模拟数据或非敏感公开 UID。
- 如果本地环境无法运行浏览器验证，要明确说明“未完成浏览器验证”，并给出已完成的替代检查。
- 报告验证结果时必须真实：写出执行过的命令、检查过的页面、是否通过，以及失败的英文错误原文。
- 不允许使用“应该可以”“大概率没问题”代替验证结果。

## 常用命令

在 PowerShell 中优先使用以下命令：

```powershell
# 查看文件列表
rg --files

# 查看目录
Get-ChildItem -Force

# 读取中文 Markdown
Get-Content -Raw -Encoding UTF8 AGENTS.md

# 搜索关键字
rg "localStorage|biliFetch|buildDays|RankView|TODO" .

# 检查是否为 Git 仓库
git status --short

# 简单静态服务，必要时用于预览 HTML
python -m http.server 8000
```

如果只需要预览单个页面，可以直接打开 HTML 文件；如果页面依赖 CDN、跨文件加载或浏览器限制，再启动本地静态服务并访问 `http://localhost:8000/`。

## Git 与提交规范

- 当前目录未检测到 Git 仓库；执行 Git 提交、查看历史、推送远端前，必须先确认 `.git` 是否存在。
- 如果用户要求提交或推送，但项目不是 Git 仓库，要直接说明当前状态，不要伪造提交结果。
- 提交前必须先检查工作区状态，区分自己修改和用户已有修改，不要回滚用户未授权的改动。
- 提交信息使用简洁中文，格式建议：

```text
类型: 简短说明
```

常见类型：

- `feat`: 新增页面、交互或整理功能
- `fix`: 修复乱码、布局、脚本错误、API 请求或本地存储问题
- `style`: 调整视觉样式，不改变行为
- `docs`: 文档变更
- `refactor`: 结构整理，不改变外部行为

- 推送远端前必须确认目标分支和远端名称；不要默认推送到 `main`、`master` 或任意远端。
- 不要把验证失败的代码提交为“完成”。如果用户要求保留阶段性提交，提交信息中要说明仍有未解决问题。

## CodeGraph

本项目的上层提示可能要求优先使用 CodeGraph 做结构化代码查询，但当前 `C:\AI_Lab\vibe_coding\biliFav-main` 尚未初始化 CodeGraph。

- 如果需要使用 CodeGraph 且工具返回 `CodeGraph not initialized`，应询问用户是否运行 `codegraph init -i`。
- 在 CodeGraph 可用时，用它回答符号、调用关系、影响范围等结构问题。
- 对静态文本、页面文案、错误字符串、CSS 颜色值等字面搜索，继续使用 `rg`。

## 每日复盘流程

当用户要求“复盘”“日报”“生成每日任务”且没有明确要求修改文件时，输出中文日报，按以下优先级排序：

1. 前台页面：页面可用性、中文显示、交互体验、移动端适配、收藏整理效率。
2. 后台管理/全栈化：从单文件原型迁移为 Vite 工程，再逐步落地 Express、Prisma、PostgreSQL。
3. 安全：避免泄露用户 UID、Cookie、token；避免把私密收藏数据上传到不可信服务。
4. 性能：减少 CDN 依赖、优化收藏拉取、分页、缓存和大列表渲染。
5. 其他：文档、部署、脚本、开发体验和项目管理。

日报必须包含：

- 当前项目结构概览；
- 三条可改进项，每条包含优先级、涉及文件、问题说明、实施步骤、验证方式；
- 两个可新增功能，每个包含作用、优先级、涉及文件、实施步骤、验证方式；
- 今日推荐执行计划；
- 明确提醒：只有用户反馈“执行第 X 项”或同等明确指令后，才开始修改项目。

## 修改流程

1. 先阅读本文件、相关 HTML/Markdown 和用户需求，不凭印象改。
2. 判断当前任务是“复盘规划”还是“执行修改”。
3. 找到最小改动范围，保持现有页面风格和项目结构。
4. 修改前说明即将改哪些文件。
5. 使用 `apply_patch` 或合适工具进行小范围编辑。
6. 修改后进行真实验证。
7. 最终回复用中文说明：改了什么、验证了什么、是否还有风险。

## 不应做的事

- 不在没有明确执行指令时修改功能代码。
- 不把当前项目迁移到 React 工程、Vite、Tailwind 构建链、TypeScript 或全栈架构，除非用户明确要求。
- 不把单文件原型拆成复杂工程结构，除非复用和维护收益非常明确，且用户同意。
- 不引入真实业务密钥、真实用户数据、浏览器 Cookie 或不可公开素材。
- 不在未验证时声称“已修复”“已完成”“测试通过”。
- 不覆盖用户已有改动，不擅自格式化无关文件。
- 不把中文需求改写成只有英文的文档或界面。
- 不清空 `biliFav:config`、`biliFav:videos`、`biliFav:overrides` 等本地数据，除非用户明确要求。

